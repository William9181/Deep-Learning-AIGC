--extra-index-url https://download.pytorch.org/whl/cpu

# 核心運算 (鎖定 numpy < 2.0，這就是報錯的主因)
numpy<2.0.0
pandas==2.1.4

# 深度學習框架 (CPU版)
torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2

# PyArrow (關鍵修正：鎖定在這個版本以配合 Streamlit 和 Datasets)
pyarrow==14.0.1

# Hugging Face 生態系 (鎖定穩定版本)
transformers==4.36.2
accelerate==0.26.1
datasets==2.16.1
peft==0.7.1
evaluate==0.4.1

# 應用程式介面
streamlit==1.30.0
plotly==5.18.0
scikit-learn==1.3.2
tabulate
